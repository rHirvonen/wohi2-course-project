info:
  name: Play Question
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/questions/1/play
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​